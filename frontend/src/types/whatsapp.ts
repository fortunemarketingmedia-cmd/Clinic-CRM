export type WhatsAppAccount = { id: string; name: string; businessAccountId: string; status: 'CONNECTED' | 'DISCONNECTED' | 'ERROR'; apiVersion: string; lastSyncAt?: string; webhookVerifiedAt?: string; lastFailureReason?: string; phoneNumbers?: WhatsAppPhoneNumber[]; _count?: { templates: number; webhookEvents: number; failureLogs: number } };
export type WhatsAppPhoneNumber = { id: string; accountId: string; branchId: string; phoneNumberId: string; displayPhoneNumber: string; normalizedPhone: string; verifiedName?: string; qualityRating?: string; messagingLimit?: string; isDefault: boolean; account?: Pick<WhatsAppAccount, 'id' | 'name' | 'status'>; branch?: { id: string; name: string } };
export type WhatsAppTemplate = { id: string; accountId?: string; branchId?: string; name: string; displayName: string; language: string; category: string; group: string; status: string; body: string; footer?: string; buttons?: Array<Record<string, unknown>>; active: boolean; sentCount: number; deliveredCount: number; readCount: number; failedCount: number; rejectionReason?: string; account?: { id: string; name: string }; branch?: { id: string; name: string } };
export type WhatsAppMessage = { id: string; direction: 'INBOUND' | 'OUTBOUND' | 'INTERNAL'; type: string; status: string; content?: string; mediaUrl?: string; templateId?: string; failureReason?: string; createdAt: string; createdBy?: { id: string; name: string } };
export type WhatsAppConversation = { id: string; normalizedMobile: string; contactName?: string; branchId: string; assignedToId?: string; status: string; priority: string; unreadCount: number; lastMessageAt: string; sessionExpiresAt?: string; person?: { id: string; fullName: string }; lead?: { id: string; name: string; status: string }; patient?: { id: string; fullName: string }; appointmentId?: string; invoiceId?: string; packageId?: string; phoneNumber: WhatsAppPhoneNumber; messages: WhatsAppMessage[]; assignedTo?: { id: string; name: string } };
export type WhatsAppBroadcast = { id: string; name: string; status: string; recipientCount: number; filteredCount: number; submittedCount: number; deliveredCount: number; readCount: number; replyCount: number; failedCount: number; optOutCount: number; scheduledAt?: string; template: WhatsAppTemplate; createdBy: { id: string; name: string }; approvedBy?: { id: string; name: string } };
export type WhatsAppAutomation = { id: string; name: string; trigger: string; branchId?: string; templateId: string; delayMinutes: number; sequenceStep: number; active: boolean; template: WhatsAppTemplate; branch?: { id: string; name: string } };
export type WhatsAppJob = { id: string; type: string; status: string; attempts: number; maxAttempts: number; runAt: string; lastError?: string; createdAt: string };
export type WhatsAppFailure = { id: string; operation: string; errorCode?: string; errorMessage: string; retryable: boolean; occurredAt: string; job?: WhatsAppJob };
